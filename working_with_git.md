## pulling data
- git clone <git_repo_name> >> clone any github repo
- git pull >> pull everything from git repo after cloning


## pushing data
- git add . >> add everything new I worked since last push
- git commit -m "<message>" >> making commit
- git push >> final pushing data from local machine to git
